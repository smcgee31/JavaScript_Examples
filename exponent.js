// This is an example of what you could do -- not the actual answer to CodeCardio


function power(base, exponent) {
    var result = 1;
        for(var count = 0;count < exponent; count++)
            result *= base
            return result;
        }

    power(2, 10);
